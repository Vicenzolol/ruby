development:
  adapter: async

test:
  adapter: test

production:
  # Usa o adapter async (in-process) no Render free tier para evitar dependência
  # de Redis. Para apps com múltiplos workers ou Turbo Streams em escala, migrar
  # para Render Key Value (Redis) ou configurar solid_cable com PostgreSQL.
  adapter: async

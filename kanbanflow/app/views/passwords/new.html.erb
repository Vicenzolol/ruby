<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4">
  <div class="card max-w-md w-full p-8 space-y-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900 text-center">Esqueci minha senha</h1>
      <p class="mt-2 text-center text-sm text-gray-500">
        Informe seu e-mail e enviaremos as instruções de redefinição.
      </p>
    </div>

    <% if alert = flash[:alert] %>
      <p class="alert-error" id="alert"><%= alert %></p>
    <% end %>

    <%= form_with url: passwords_path, class: "space-y-4" do |form| %>
      <div>
        <%= form.label :email_address, "E-mail", class: "label" %>
        <%= form.email_field :email_address, required: true, autofocus: true,
            autocomplete: "username", placeholder: "seu@email.com",
            value: params[:email_address], class: "input" %>
      </div>

      <%= form.submit "Enviar instruções", class: "btn btn-primary w-full" %>

      <p class="text-center text-sm">
        <%= link_to "Voltar para o login", new_session_path, class: "text-gray-500 hover:text-gray-700 underline" %>
      </p>
    <% end %>
  </div>
</div>

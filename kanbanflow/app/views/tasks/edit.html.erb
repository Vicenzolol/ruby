<div class="max-w-lg mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-1">Editar Tarefa</h1>
  <p class="text-sm text-gray-500 mb-6">Projeto: <%= @task.project.name %></p>
  <%= render "form", task: @task %>
  <div class="mt-4">
    <%= link_to "Cancelar", @task.project, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
</div>

<div class="max-w-lg mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-1">Nova Tarefa</h1>
  <p class="text-sm text-gray-500 mb-6">Projeto: <%= @project.name %></p>
  <%= render "form", task: @task %>
  <div class="mt-4">
    <%= link_to "Cancelar", @project, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
</div>
